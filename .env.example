### SasaMum example environment variables
# Copy this file to `.env` and fill in real values for local development.
#
# Note: SMTP settings are optional for local development. If SMTP_* variables are
# not set, the backend will fall back to logging verification links to the console.

# SMTP (optional) - set these to enable real outgoing emails
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
SMTP_FROM="SasaMum <no-reply@example.com>"

# Backend base URL used to build verification links (example for local dev)
# If your backend runs on a different port update this accordingly.
BACKEND_URL=http://localhost:3001

# JWT secret (development only). Use a secure secret in production.
JWT_SECRET=change_this_dev_secret

# Database (example Postgres connection string)
# Update to match your local development database.
DATABASE_URL=postgresql://postgres:Omondijr69!!@localhost:5432/SasaMum?schema=public